export default class SpellChecker {
    private dict: string[];

    constructor({ dict: string }: any) {
        this.dict = this.dict;
    }

    public process(input: string): string {
        return '';
    }
}
